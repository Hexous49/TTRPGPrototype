export class MyTTRPGActor extends Actor {
  prepareBaseData() {
    super.prepareBaseData();
  }

  prepareDerivedData() {
    super.prepareDerivedData();
    // Calculate derived values here as the system grows
  }
}
